# Evidence Templates (Class 1 — committed, synthetic only)

One `*.template.md` per record kind. Templates and SYNTHETIC examples only —
real identifiers, dates tied to real events, hostnames, or tenant data never
appear here. Copy a template to a machine-local location (class 2) to execute
it; never fill one in place.
