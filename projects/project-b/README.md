# project-b (placeholder)

Reserved slot for the next sub-project. To provision it, copy the structure of
`../project-a/` (constitution, `docs/specs/template/`, `knowledge/instructions.md`,
`reference/`) and rewrite its constitution for the new scope. See the root
`README.md` for the full provisioning guide.
