# CodeRabbit Finding Disposition — Phase 2 Planning Package

**Feature Folder**: `002-centcom-phase-2-live-github-ingestion` | **Date**: 2026-07-23 | **Round**: CodeRabbit (VS Code extension) review of the R1-revised package plus workspace tooling, 15 findings relayed by the Workspace Maintainer (Agent Zero)

**Standing**: this disposition records corrections directed by Agent Zero ("defects … that require correction", 2026-07-23). It grants and claims no approval; planning approval (Stage A) remains pending, Gate 2 (Stage B) has been neither requested nor granted, and the R1 stop conditions (no commits/pushes/PRs/merges of this package) remain in force. Same pattern as `r1-finding-disposition.md`.

## Dispositions (all 15 Accepted)

| # | Target | Finding | Resolution |
|---|--------|---------|------------|
| 1 | quickstart.md Step 7 | Replay coequal with a live change overclaims FR-006's second half | One real default-branch change now required to accept FR-006's second half; replay demoted to supplemental (validates FR-014/FR-015 only); substitution leaves the criterion open and Step 15 records the gap. Proves line and coverage row aligned. |
| 2 | quickstart.md Step 3.3 | Head-at-observation-time not provable from current history alone | Claim narrowed: the ingestion-run row is the contemporaneous witness; current history proves SHA resolution/branch membership only; public-history spot-check demoted to corroboration. |
| 3 | contracts/provenance-envelope.md | `observedAt` regex accepts calendar-impossible dates | `format: date-time` added; validator MUST enable format assertion (Ajv + ajv-formats); §3 rule added; C-ENV-2 extended with a calendar-invalid case; constituent inherits via `$ref`. |
| 4 | contracts/provenance-envelope.md | Examples declared `authoritative: true` with no recorded authority decision | First-slice authority policy defined (§3): live results declare `authoritative: false` with `governanceWarning` until a dated Agent Zero authority decision is recorded. Both examples corrected; C-ENV-4 and the §4 T014-enforcement list extended; quickstart Step 4.2 aligned. **New open decision for Agent Zero** (see Open items). |
| 5 | .agents/skills/governed-change/scripts/commit-governed.sh | Invariants unenforced (index-only check; commit before verify; unverified postconditions) | Rewritten: dirty-paths-within-declared-set precondition, verify-layout runs before the commit, post-commit asserts committed paths within the declared set plus a clean tree — all hard failures. Hardened further after adversarial review (see Verification). |
| 6 | .codex/hooks.json | Machine-absolute hook paths embedding a username | Both commands resolve the repository root at runtime (`git rev-parse --show-toplevel`). |
| 7 | .codex/hooks/record-mining-reminder.sh | Path match far broader than the documented tiers | Rewritten: separator normalization, repo-root-anchored match on exactly `conversations/<file>.md` and `projects/<name>/conversations/<file>.md`, terminal `.md`, no nesting; scaffolding excluded by exact filename. |
| 8 | .codex/hooks/skill-reminder.sh | Advisories pointed at `.claude/skills/` instead of the canonical `.agents/skills/` | Both advisory strings corrected to `.agents/skills/…` (referenced SKILL.md files verified present). |
| 9 | contracts/snapshot-collection.md §8 | No serving behavior defined for a PostgreSQL outage | New policy P-3a: bundled fixture dataset served via the P-3 path with full fixture labeling and an outage governance warning; monitoring surfaced (FR-028); explicit labeled error if the fixture itself is unloadable. New scenario C-SNAP-10; coverage rows updated; quickstart Step 9.3, test-strategy §2.7, tasks T008 aligned. |
| 10 | data-model.md | Concurrent hourly/on-demand runs can regress the anchor or publish out of order | E-02: single-flight serialization per collection (`pg_advisory_xact_lock` or equivalent, bound at T015; observation timestamp injected after lock acquisition). E-08: database-side monotonic guard (T013 migration; snapshot row commits in the producing run's transaction). S-01 invariant 6 added; snapshot-collection V-6, tasks T013/T015, test-strategy §2.9, risk R-08 aligned. |
| 11 | spec.md FR-021 | Contradicted Q-004's public-class dashboard exposure | FR-021 narrowed: authorization required before non-public data — or live data beyond the explicitly public-class snapshot fields cleared under Q-004/FR-027 — is exposed. Mirrored in data-model E-09, traceability FR-021 row, risk R-03. |
| 12 | tasks.md / gate-2-entry-criteria.md | T011/G-09 authorization deadlock | G-09's pre-Gate-2 evidence comes from a workspace-side compilation run against the contract-embedded schemas; T011 ([EXT], blocked on the Gate 2 directive) is the Stage C CI counterpart only, never a Gate 2 dependency. tasks.md, gate-2-entry-criteria.md, test-strategy §2.2, implementation-authorization-boundary.md aligned. |
| 13 | test-strategy.md | C-SNAP-9 conflated with implementation-gated contract tests | §2.1 pass condition carves out C-ENV-8 and C-SNAP-9; T005 = C-SNAP-1..8 + C-SNAP-10; snapshot contract status line and §9 preamble gained the matching C-SNAP-9 exception; all scenario-range citations updated. |
| 14 | traceability.md D-R1-12 | Claimed the R1 disposition lives outside the folder; the file is in-folder | D-R1-12 and GAP-4 corrected (disposition in-folder; revised handoff by process); Table 5 notes the ninth in-folder R1 artifact; D-10-1 phrasing tightened to "eight §6 deliverables". |
| 15 | quickstart.md Step 14.1 | Zero-secret scan scope included the required server-side secrets | Scan scoped to client-reachable and committed surfaces; required server-side secrets (read-only live-source token, PostgreSQL credentials in Edge Function secret storage) excluded from the zero count by design and verified by inspection; a required secret in any zero-secret surface is a scan failure. test-strategy §2.11, tasks T022, traceability FR-024 aligned. |
| 16 | coderabbit-finding-disposition.md (re-review) | Contract-version state ambiguous: normative fixes applied under unchanged 1.1.0 `$id`s — consumers cannot tell whether the fixes are in 1.1.0 or pending | Accepted; Agent Zero reversed the hold and approved the coordinated bump. Both contracts 1.1.0 → 1.2.0: headers, changelog rows, schema `$id` URNs, envelope `$ref`, and every current-version cross-reference in spec-package artifacts (plan, quickstart, data-model, research, risk-register, traceability, tasks, test-strategy, gate-2-entry-criteria) updated in one pass; historical v1.1.0 citations preserved. |

## Verification

- Scripts: `bash -n` clean; `hooks.json` valid JSON; functional pipe-tests pass for both hooks (tier anchoring incl. JSON-escaped and lowercase-drive paths, scaffolding exact-match, vendor/`.md.bak` rejection, `.agents` advisory text).
- Adversarial verification sweep (5 independent agents) over all 15 fixes and the full artifact set: 24 residual issues found (3 blocking) — all corrected in the same round, including stale C-SNAP ranges, blanket FR-021/secret-scan restatements in sibling artifacts, T013/T015 not carrying the serialization/guard design, a timeline impossibility in the mixed worked example, and script hardening (`-uall`, `core.quotePath=false`, absolute message-file resolution, rename-extraction order, case-insensitive root-prefix match, exact scaffolding filenames, dead skills-creator pointer).
- Envelope schema and all example instances re-validated (JSON parse plus hand-executed schema constraints, including all conditionals): clean.

## Open items for Agent Zero

1. **First-slice authority decision** (finding 4): a dated record naming the live `get_snapshot` collection and the authority granted. Until it exists, live results serve `authoritative: false` with a governance warning.
2. ~~Contract version bump — held for Stage A packaging~~ **RESOLVED (2026-07-23)**: finding 16 (below) surfaced the `$id` ambiguity of applying normative changes under unchanged version identifiers; Agent Zero approved completing the coordinated bump immediately. Both contracts advanced 1.1.0 → 1.2.0 with changelog rows recording this round; `$id` URNs, the envelope `$ref`, and every cross-reference across the package updated in one pass (historical v1.1.0 citations — changelogs, R1-origin annotations, `r1-finding-disposition.md` — deliberately preserved).

---

*This disposition is a planning artifact. It records remediation of externally reported findings under Agent Zero's correction directive; it grants no planning approval, requests no Gate 2, and leaves the R1 stop conditions in force.*
