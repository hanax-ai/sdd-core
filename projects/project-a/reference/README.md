# Hana-X-Subsystem Reference

Project-specific mocks, schemas, and narrow code slices — e.g., SAP S/4 HANA
OData metadata exports and mock payloads. Long documents MUST follow the
slicing rules in `../knowledge/instructions.md` (part-NN slices plus an
`_index.md` manifest).
