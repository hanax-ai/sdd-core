# Global Reference

System design records, database schemas (PostgreSQL / Qdrant), upstream API
contracts, and third-party mirrors shared across all sub-projects.

- `repos/` — local clones of external frameworks (git-ignored). Every mirror
  MUST be registered in `../knowledge/instructions.md` before agents may cite it.
